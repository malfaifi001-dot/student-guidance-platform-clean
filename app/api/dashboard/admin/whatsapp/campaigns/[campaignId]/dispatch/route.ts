import { NextResponse } from "next/server"; import { requireAdminApi } from "@/lib/admin/admin-api-guard"; import { getCurrentSessionUser } from "@/lib/auth/current-user"; import { logAdminActivity } from "@/lib/admin/activity-log"; import { dispatchCampaignBatch } from "@/lib/whatsapp/whatsapp-campaign-service";
export const dynamic="force-dynamic";
export async function POST(_request:Request,ctx:{params:Promise<{campaignId:string}>}){const denied=await requireAdminApi();if(denied)return denied;const {campaignId}=await ctx.params;const result=await dispatchCampaignBatch(campaignId);const current=await getCurrentSessionUser();await logAdminActivity({actorUserId:current?.user?.id||null,category:"COMMUNICATION",action:"whatsapp-campaign-dispatched",severity:"INFO",title:"تمت معالجة دفعة من حملة واتساب",details:{campaignId,...result}});return NextResponse.json(result);}
