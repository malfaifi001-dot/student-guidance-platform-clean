import { NextResponse } from "next/server"; import { requireAdminApi } from "@/lib/admin/admin-api-guard"; import { getCurrentSessionUser } from "@/lib/auth/current-user"; import { logAdminActivity } from "@/lib/admin/activity-log"; import { markConversationRead } from "@/lib/whatsapp/whatsapp-service";
export const dynamic="force-dynamic";
export async function PATCH(_request:Request,ctx:{params:Promise<{conversationId:string}>}){const denied=await requireAdminApi();if(denied)return denied;const {conversationId}=await ctx.params;const conversation=await markConversationRead(conversationId);const current=await getCurrentSessionUser();await logAdminActivity({actorUserId:current?.user?.id||null,category:"COMMUNICATION",action:"whatsapp-conversation-marked-read",severity:"INFO",title:"تم تعليم محادثة واتساب كمقروءة",details:{conversationId}});return NextResponse.json({conversation});}
