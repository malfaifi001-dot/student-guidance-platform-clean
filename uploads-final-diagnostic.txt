============================================================
FILE: app\uploads\evidence\[fileName]\route.ts
============================================================
import { readFile } from "fs/promises";
import { NextResponse } from "next/server";

import {
  getEvidenceMimeType,
  isSafeEvidenceStoredFileName,
  resolveExistingEvidenceFile,
} from "@/lib/evidence/evidence-file-storage";

export const runtime = "nodejs";
export const dynamic = "force-dynamic";

type Context = { params: Promise<{ fileName: string }> };

function notFound() {
  return new NextResponse("Not Found", {
    status: 404,
    headers: {
      "Cache-Control": "no-store",
      "Content-Type": "text/plain; charset=utf-8",
      "X-Content-Type-Options": "nosniff",
    },
  });
}

export async function GET(_request: Request, context: Context) {
  const { fileName } = await context.params;
  if (!isSafeEvidenceStoredFileName(fileName)) return notFound();

  const contentType = getEvidenceMimeType(fileName);
  const filePath = await resolveExistingEvidenceFile(fileName);
  if (!contentType || !filePath) return notFound();

  try {
    const file = await readFile(filePath);
    return new NextResponse(new Uint8Array(file), {
      status: 200,
      headers: {
        "Cache-Control": "public, max-age=31536000, immutable",
        "Content-Disposition": `inline; filename="${fileName}"`,
        "Content-Length": String(file.byteLength),
        "Content-Type": contentType,
        "X-Content-Type-Options": "nosniff",
      },
    });
  } catch {
    return notFound();
  }
}


============================================================
EVIDENCE WRITE / USER SIGNATURE ROUTE SEARCH
============================================================
D:\student-guidance-platform-clean\app\api\dashboard\settings\school\signature\route.ts:41
    const relativeDirectory = path.join("user-signatures", input.userId);

D:\student-guidance-platform-clean\app\uploads\evidence\[fileName]\route.ts:7
  resolveExistingEvidenceFile,

D:\student-guidance-platform-clean\app\uploads\evidence\[fileName]\route.ts:31
  const filePath = await resolveExistingEvidenceFile(fileName);

D:\student-guidance-platform-clean\lib\evidence\delete-evidence-files.ts:5
  getEvidenceStoredFileNameFromUrl,

D:\student-guidance-platform-clean\lib\evidence\delete-evidence-files.ts:11
      .map((fileUrl) => fileUrl ? getEvidenceStoredFileNameFromUrl(fileUrl) : null)

D:\student-guidance-platform-clean\lib\evidence\evidence-file-storage.ts:63
export async function resolveExistingEvidenceFile(fileName: string) {

D:\student-guidance-platform-clean\lib\evidence\evidence-file-storage.ts:74
export async function writeEvidenceFile(fileName: string, data: Uint8Array) {

D:\student-guidance-platform-clean\lib\evidence\evidence-file-storage.ts:102
export function getEvidenceStoredFileNameFromUrl(fileUrl: string) {

D:\student-guidance-platform-clean\lib\evidence\save-evidence-files.ts:4
import { writeEvidenceFile } from "@/lib/evidence/evidence-file-storage";

D:\student-guidance-platform-clean\lib\evidence\save-evidence-files.ts:113
    await writeEvidenceFile(storedName, buffer);

